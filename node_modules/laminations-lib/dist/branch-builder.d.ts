import { Chord } from './chords';
import { Fraction } from './fractions';
import { BranchRegion } from './branch-region';
export interface BranchSpec {
    chord: Chord;
    endpoints: Fraction[];
    flip?: boolean;
}
export declare const makeBranchSpec: (chord: Chord, ...endpoints: Fraction[]) => {
    chord: Chord;
    endpoints: Fraction[];
};
export declare const buildBranches: (specs: BranchSpec[]) => BranchRegion[];
export declare const maybeAddFinalBranch: (base: number, regions: BranchRegion[]) => BranchRegion[];
export declare const makeBuilder: (base: number) => (specs: BranchSpec[]) => BranchRegion[];
