import { Fraction } from './fractions';
import { Chord } from './chords';
import { List } from 'immutable';
export interface Polygon {
    points: List<Fraction>;
}
export declare const Polygons: {
    create: (points: List<Fraction>) => Polygon;
    fromChord: (chord: Chord) => Polygon;
    mapForward: (polygon: Polygon) => Polygon;
    toChords: (polygon: Polygon) => List<Chord>;
    toString: (polygon: Polygon) => string;
};
